import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Audit() {
  return (
    <AIPageContainer
      title="Document Audit Trail"
      description="Track changes and access history of your documents."
    />
  );
}
