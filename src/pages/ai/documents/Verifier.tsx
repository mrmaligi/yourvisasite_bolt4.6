import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Verifier() {
  return (
    <AIPageContainer
      title="Authenticity Verifier"
      description="Verify the authenticity of your documents."
    />
  );
}
