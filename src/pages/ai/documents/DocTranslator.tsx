import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function DocTranslator() {
  return (
    <AIPageContainer
      title="Document Translator"
      description="Translate entire documents while preserving formatting."
    />
  );
}
