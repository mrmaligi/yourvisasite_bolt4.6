import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Extraction() {
  return (
    <AIPageContainer
      title="Entity Extraction"
      description="Extract named entities from text using ML."
    />
  );
}
