import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Triggers() {
  return (
    <AIPageContainer
      title="Event Triggers"
      description="Configure triggers to start your workflows."
    />
  );
}
