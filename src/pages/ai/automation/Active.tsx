import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Active() {
  return (
    <AIPageContainer
      title="Active Workflows"
      description="Monitor currently running automation processes."
    />
  );
}
