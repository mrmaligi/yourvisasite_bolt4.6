import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Templates() {
  return (
    <AIPageContainer
      title="Automation Templates"
      description="Pre-built templates for common immigration tasks."
    />
  );
}
