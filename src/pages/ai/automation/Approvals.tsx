import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Approvals() {
  return (
    <AIPageContainer
      title="Approval Queues"
      description="Manage tasks requiring manual approval."
    />
  );
}
