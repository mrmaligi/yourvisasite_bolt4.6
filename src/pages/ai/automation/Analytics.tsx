import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Analytics() {
  return (
    <AIPageContainer
      title="Workflow Analytics"
      description="Analyze the efficiency of your automations."
    />
  );
}
