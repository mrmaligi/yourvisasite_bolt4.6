import { AIPageContainer } from '../../../components/ai/AIPageContainer';

export function Competitor() {
  return (
    <AIPageContainer
      title="Global Comparison"
      description="Compare visa options across different countries."
    />
  );
}
